function scr_space2(){
		
if (hsp != 0) image_xscale = sign(hsp);

	//dash atma
	dashduration = max(dashduration -1, 0);
if (space) && !place_meeting(x + image_xscale*64, y, obj_char1)
{
	dashduration = 10;
	hsp = image_xscale * dashspeed;
}
}